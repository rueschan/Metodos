<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="metodos.rueschan.mx.metodos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Métodos Numéricos"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GaussInput"
            android:label="Gauss" />
        <activity android:name=".LagrangeInput" />
        <activity android:name=".MinCuadInput" />
        <activity android:name=".MetodoCramer" />
        <activity android:name=".Cramer" />
        <activity
            android:name=".GaussResult"
            android:label="Resultado" />
        <activity
            android:name=".Jordan"
            android:label="Gauss-Jordan" />
        <activity
            android:name=".Inversa"
            android:label="Inversa" />
    </application>

</manifest>